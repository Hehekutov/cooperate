{
  "name": "Backend",
  "description": "Fastify backend for the cooperate idea platform",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node src/seed.js",
    "test": "node --test"
  },
  "dependencies": {
    "fastify": "^5.8.2"
  }
}
